<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="github.kaierwen.androiddevlibrary">

    <!-- Tell the system this app requires OpenGL ES 2.0. -->
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:name=".base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".openlibrary.KotlinActivity" />
        <activity android:name=".widgets.CustomViewActivity" />
        <activity android:name=".widgets.OpenGLActivity" />
        <activity android:name=".widgets.RoundViewActivity" />
        <activity android:name=".openlibrary.X5WebViewActivity" />
        <activity android:name=".camera.MyCameraDemo" />
        <activity android:name=".camera.AndroidResizeImageActivity" />
        <activity
            android:name=".widgets.DashedLineViewAcitivty"
            android:label="@string/title_widget_dashed_line" />
        <activity
            android:name=".widgets.TintActivity"
            android:label="@string/title_tint" />
        <activity
            android:name=".widgets.FirstViewWrapSecondViewWrapActivity"
            android:label="@string/title_first_view" />
        <activity
            android:name=".widgets.EmojiCompatActivity"
            android:label="@string/title_emoji_compat" />
        <activity
            android:name=".download.DownloadDemo"
            android:label="@string/title_download_demo" />
    </application>

</manifest>